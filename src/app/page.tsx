"use client";

import { PortalLayout } from "@/components/PortalLayout";

export default function DocumentsPage() {
  return (
    <PortalLayout>
      <p style={{ color: "var(--tellian-stone)", marginTop: "48px" }}>
        Dokumenten-Liste wird in Commit (c) gebaut.
      </p>
    </PortalLayout>
  );
}
